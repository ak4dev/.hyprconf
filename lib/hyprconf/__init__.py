"""hyprconf — the Omarchy overlay's Firefox theme bridge (firefox_theme).

`__version__` is a copy of the top-level VERSION file, which `scripts/publish`
reads; nothing in the overlay reads this one.
"""

__version__ = "7.0.0"
